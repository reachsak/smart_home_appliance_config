import { CommandType } from "./enums";
export declare class Command {
    id: number;
    method: CommandType;
    params: any[];
    constructor(id: number, method: CommandType, params: any[]);
    getString(): string;
}
